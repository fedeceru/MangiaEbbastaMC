import { View, Text } from "react-native"

const OrderStatusScreen = () => {
    //questa schermata mostra lo stato dell'ordine, in particolare una mappa che tiene traccia della posizione del drone 
    //il tmpo impiegato da esso per consegnare l'ordine e lo stato dell'ordine stesso  

    return (
        <View>
            <Text>Order Status Screen</Text>
        </View>
    )
}

export default OrderStatusScreen;